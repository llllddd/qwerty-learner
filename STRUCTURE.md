# Overview

Based on the files provided, here is an explanation of the project **qwerty-learner**.

It appears to be a **React-based web application** designed to help users learn English vocabulary (specifically the Oxford 5000 list) and likely practice typing simultaneously.

Here is a breakdown of the two files:

## 1. `public/dicts/Oxford5000.json`

This is a **data source file** containing the vocabulary content for the application.

- **Format**: A JSON array of objects.
- **Content**: It lists English words from the "Oxford 5000" (a list of the most important words to learn in English).
- **Structure**: Each object represents a word and contains:
  - `name`: The English word (e.g., "abandon").
  - `trans`: An array containing translations, definitions, parts of speech, and sometimes verb tenses or noun forms.
  - `usphone`: The US phonetic transcription.
  - `ukphone`: The UK phonetic transcription.

## 2. `yarn.lock`

This is a **dependency lock file** generated by the Yarn package manager. It reveals the technical stack and libraries used to build the application.

- **Core Framework**: **React** (`react`, `react-dom`) is the UI library, using **Vite** (`vite`) as the build tool for fast development.
- **Language**: **TypeScript** is used, evidenced by the numerous `@types/*` packages and the `typescript` dependency.
- **Styling & UI**:
  - **Tailwind CSS** (`tailwindcss`) for utility-first styling.
  - **Radix UI** (`@radix-ui/*`) and **Headless UI** (`@headlessui/react`) for unstyled, accessible UI primitives (like dialogs, tooltips, toggles).
  - **Lucide React** (`lucide-react`) for icons.
- **State & Data**:
  - **Jotai** (`jotai`) for global state management.
  - **SWR** (`swr`) for data fetching.
  - **Dexie.js** (`dexie`) is a wrapper for IndexedDB, suggesting the app saves user progress or settings locally in the browser.
- **Audio**: **Howler.js** (`howler`, `use-sound`) is used, likely to play the pronunciation of words or game sound effects.
- **Visualization**: **ECharts** (`echarts`) is included, probably to display learning statistics or typing speed progress charts.
- **Testing**: **Playwright** (`@playwright/test`) is used for end-to-end testing.

## Summary

**Qwerty Learner** is a modern Single Page Application (SPA) that combines vocabulary building with typing practice. It uses a local database to track progress, plays audio for pronunciation, and visualizes stats, all built on a robust React + Vite + TypeScript stack.
